name: build

on:
  workflow_dispatch:
  push:
    branches: [ "main", "DEVOPS-7450-deployment" ]
  pull_request:
    branches: [ "main" ]

env:
  GO_VERSION: "1.23"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ env.GO_VERSION }}

    - name: Build
      run: make

    - name: Test
      run: make test

    - name: Lint
      run: make lint
      continue-on-error: true
