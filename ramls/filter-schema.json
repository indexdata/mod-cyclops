{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "A filter in a CYCLOPS system, used to capture a filtering criterion",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The short human-readable name of the filter"
    },
    "jsonCond": {
      "description": "A WHERE condition expressed as a structure rather than in CCMS's command language. The service validates it and generates the condition itself, so values are never interpreted as syntax.",
      "$ref": "cond-schema.json"
    },
    "template": {
      "type": "string",
      "description": "Optionally, the name of another filter to duplicate"
    }
  },
  "additionalProperties": false,
  "required": [
    "name"
  ]
}
