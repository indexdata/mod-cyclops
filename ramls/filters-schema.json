{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "An object containing a list of zero or more filter names",
  "type": "object",
  "properties": {
    "filters": {
      "description": "The names of the known filters (the 'cond' and 'template' of each filter are not included in this listing)",
      "type": "array",
      "items": {
        "type": "string",
        "description": "The short human-readable name of a filter"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "filters"
  ]
}
